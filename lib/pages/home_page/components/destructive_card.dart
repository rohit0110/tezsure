import 'package:flutter/material.dart';

class DestructiveCard extends StatelessWidget {
  const DestructiveCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 100,
      color: Colors.grey,
    );
  }
}
